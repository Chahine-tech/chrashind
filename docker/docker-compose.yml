version: '3'

services:
  database:
    container_name: chrasshind
    hostname: chrasshind
    image: postgres:14.2
    restart: unless-stopped
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: database